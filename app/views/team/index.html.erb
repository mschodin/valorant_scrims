<% if user_signed_in? %>
  <% user_id = current_user.id %>
<% else %>
  <% user_id = nil %>
<% end %>

<%= react_component("Teams", {
  create_team_path: team_index_path,
  signed_in_user: user_id,
})%>